<%= form_with(model: q, local: true) do |form| %>
  <% if q.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(q.errors.count, "error") %> prohibited this q from being saved:</h2>

      <ul>
      <% q.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :student_id %>
    <%= form.text_field :student_id %>
  </div>

  <div class="field">
    <%= form.label :questionnaire_id %>
    <%= form.text_field :questionnaire_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
